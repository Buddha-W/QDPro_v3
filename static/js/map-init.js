document.addEventListener('DOMContentLoaded', function() {
    console.log("DOM loaded, checking for map...");

    // Ensure we don't have any stray closing parentheses that might cause syntax errors

});